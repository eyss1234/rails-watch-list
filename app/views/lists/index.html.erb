<div>
  <%= render "partials/banner" %>
  <div class="container mt-5">
  <ul class="cards">
    <% @lists.each do |list| %>
      <div class="card-category"><%= link_to list.name, list_path(list)%></div>
    <% end %>
  </ul>
  </div>
</div>

<%= cl_image_tag("relaxing_wvkjzh", width: 400, height: 300, crop: :fill) %>